eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultzone: http://localhost:8761/eureka/

server:
  port: 8090

spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true  # uncomment to enable SQL logging
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/db_transaksi?useTimeZone=true&serverTimezone=UTC&autoReconnect=true&useSSL=false
    username: kmds
    password: password
    driver-class-name: com.mysql.jdbc.Driver